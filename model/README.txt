Place your yolov10n.onnx model file here. If you want DeepSORT appearance embeddings, add an embedding model and integrate in yolov10.js.

Suggested: MobileNet-based embedding in ONNX, compute a 128-d vector per crop, then use cosine distance in matching.